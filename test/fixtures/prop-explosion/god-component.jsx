import React from 'react';

const GodComponent = ({
  title, subtitle, description, icon, color, size, variant,
  disabled, loading, error, onSubmit, onCancel, onReset
}) => {
  return <div>{title}</div>;
};

export default GodComponent;
